#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n = 0;
    vector<int> arr(n);
    for(int i = 0; i <= n; ++i){
        cin >> arr[i];
        arr.resize(n+1);
    }
    vector<int> lattice(arr[0]*arr[0]);
    for(int i = 0; i < arr.size()0; ++i){
        
    }
    return 0;
}