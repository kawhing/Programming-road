#include <bits/stdc++.h>
using namespace std;

void solve(){
    int a, b, c, ans = 0;
    cin >> a >> b >> c;
    ans = a * 0.2 + b * 0.3 + c * 0.5;
    cout << ans << endl;
}

int main(){
    solve();
    return 0;
}